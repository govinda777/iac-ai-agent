# =====================================================
# VARIÁVEIS OBRIGATÓRIAS
# =====================================================

# 1. PRIVY.IO
PRIVY_APP_ID=cmgh6un8w007bl10ci0tgitwp

# 2. NATION.FUN NFT
WALLET_ADDRESS=0x17eDfB8a794ec4f13190401EF7aF1c17f3cc90c5
# NÃO USE WALLET_PRIVATE_KEY - Por razões de segurança, chaves privadas não são mais suportadas diretamente

# WALLET_TOKEN= # Opcional: gerado automaticamente se não definido
# NATION_NFT_CONTRACT= # Opcional: valor padrão usado se não definido
NATION_NFT_REQUIRED=true

# 3. LLM API (Nation.fun - autenticação via carteira Web3)
LLM_PROVIDER=nation.fun
LLM_MODEL=nation-1
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=4000

# =====================================================
# VARIÁVEIS RECOMENDADAS
# =====================================================

# 4. BASE NETWORK (opcional - valores padrão serão usados)
# BASE_RPC_URL=https://goerli.base.org
# BASE_CHAIN_ID=84531

# 5. WHATSAPP API (opcional - protegido por Lit Protocol)
# WHATSAPP_API_KEY= # Será armazenado de forma segura usando Lit Protocol

# =====================================================
# VARIÁVEIS OPCIONAIS
# =====================================================

# 6. FEATURES
ENABLE_NFT_ACCESS=true
ENABLE_TOKEN_PAYMENTS=true
ENABLE_STARTUP_VALIDATION=true
ENABLE_WEB3_AUTH=true

# 7. SERVER
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=development

# 8. LOGGING
LOG_LEVEL=info
LOG_FORMAT=json

# 9. RATE LIMITING
BASIC_TIER_RATE_LIMIT=10
PRO_TIER_RATE_LIMIT=100
ENTERPRISE_TIER_RATE_LIMIT=1000

# 10. GITHUB (Secrets via Git Secrets)
# Use: git secret add github_token
# Use: git secret add github_webhook_secret

# 11. MONITORING (Opcional)
# SENTRY_DSN=
# DATADOG_API_KEY=
